# front-app

Auth-focused frontend module.

Includes:
- Auth routes (`app/auth`) like login, register, otp, success, and complete-profile
- Auth UI/components
- OTP/auth hooks and services
- Auth i18n files
